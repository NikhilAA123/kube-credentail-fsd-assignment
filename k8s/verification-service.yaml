# k8s/verification-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: verification-service
  namespace: kube-credential-ns
spec:
  type: ClusterIP # Change this back
  selector:
    app: verification-service
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
