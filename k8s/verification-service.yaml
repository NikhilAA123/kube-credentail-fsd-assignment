apiVersion: v1
kind: Service
metadata:
  name: verification-service
  namespace: kube-credential-ns
spec:
  type: ClusterIP
  selector:
    app: verification-service
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
