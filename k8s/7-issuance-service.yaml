apiVersion: v1
kind: Service
metadata:
  name: issuance-service
  namespace: kube-credential-ns
  labels:
    app: issuance-service
spec:
  type: ClusterIP
  selector:
    app: issuance-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
