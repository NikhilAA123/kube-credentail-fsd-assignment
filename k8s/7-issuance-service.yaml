apiVersion: v1
kind: Service
metadata:
  name: issuance-service
  namespace: kube-credential-ns
spec:
  type: ClusterIP
  selector:
    app: issuance-service
  ports:
    - port: 8081
      targetPort: 8081
