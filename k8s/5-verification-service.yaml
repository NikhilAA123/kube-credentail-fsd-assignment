apiVersion: v1
kind: Service
metadata:
  name: verification-service
  namespace: kube-credential-ns
spec:
  type: ClusterIP
  selector:
    app: verification-service
  ports:
    - port: 8082
      targetPort: 8082
